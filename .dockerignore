# Dependencies are reinstalled inside the image via `npm ci`
node_modules

# Build output is regenerated by `npm run build`
dist
dist-ssr

# Secrets must come from the host's environment, never be baked into a layer
.env
.env.*
!.env.example

# Local SQLite database — production uses the /data volume
*.db
*.db-journal
*.sqlite
*.sqlite3

.git
.github
.vscode
.idea

npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

.DS_Store
Thumbs.db
